/// <reference types="vite/client" />

// Gives the renderer types for Vite's `import.meta.env` (e.g. `import.meta.env.DEV`,
// which gates the pill's dev-only state switcher out of production builds).
