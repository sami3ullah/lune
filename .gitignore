# Dependencies
node_modules/

# Build output
dist/
out/
# The whisper-server binary is built into the repo-root build/ (scripts/build-whisper-server.sh).
# Root-anchored so it never shadows apps/desktop/build/, electron-builder's tracked signing
# resources (entitlements). The models/weights are never here - Provisioning downloads them.
/build/
# electron-builder packaging output (the .app and DMG), at any package level.
release/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# Editors
.vscode/
.idea/

# Environment / secrets
.env
.env.*

# v1 code kept locally only as a porting reference (DECISIONS.md #10); never tracked
_legacy/

# Local planning scratchpad (issues/tickets, session docs)
.scratch/

# Claude Code harness
.claude/
